import styled from 'styled-components'
import { variant } from 'styled-system'

const variants = variant({
  variants: {
    primary: {
      background: 'blues.med',
      color: 'white',
      display: 'flex',
      py: 1,
      textAlign: 'center'
    },
    action: {}
  }
})
